{
  "name": "ember-workspace",
  "version": "1.0.0",
  "description": "An example of a pnpm workspace for ember.",
  "main": "index.js",
  "scripts": {
    "start": "pnpm --stream -r start",
    "start:dev": "pnpm --stream -r run start:dev",
    "clean": "rm -rf .parcel-cache && pnpm -r run clean",
    "build": "pnpm --stream -r build",
    "host": "pnpm --stream -r host"
  },
  "keywords": [],
  "license": "MIT"
}
